name: browser_style_tabs_example
description: Example app embedding GeniusLink BrowserStyleTabBar in a realistic workspace shell.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  browser_style_tabs:
    path: ../

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
